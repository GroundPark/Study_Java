import java.net.*;
import java.io.*;
public class Network2 {

	public static void main(String[] args)  {
		// TODO URL(Uniform Resource Locator) : 인터넷에 존재하는 서버들이 제공하는 자원에 접근할 수 있는 주소를 표현하기 위한 것
		// 프로토콜://호스트명(:포트번호)/경로명/파일명(?쿼리스트링)(#참조)      ; ()는 생략가능
		
		/*  예제 URL 에서... http://www.naver.com:80/sample/main.html?ref=mine#idx1
		 *  프로토콜 : 통신에 사용되는 통신 규약 (http)
		 *  호스트명 : 자원 제공 서버의 이름 (www.naver.com)      
		 *  포트번호 : 서버의 포트번호 (http에서 80을 써서 거의 생략함)
		 *   경로명 : 자원이 저장된 서버상 위치 (/sample/)
		 *   쿼리    : ? 이후
		 *   참조    : # 이후
		 */
		
		URL testURL = new URL("http://www.naver.com:80/sample/main.html?ref=mine#idx1");
		
		System.out.println("getProtocol : " +testURL.getProtocol());
		System.out.println();
		System.out.println();
		System.out.println();
		
		
		
		
		
		
		// TODO 자바에서 제공하는 URL 클래스를 사용하는 예제
		//URL url = new URL("");
		
		URL url = null;
		BufferedReader input = null;
		String address = "";
		String line = "";
		
		try {
			url = new URL(address);
			input = new BufferedReader(new InputStreamReader(url.openStream()));
			
			while( (line=input.readLine()) != null ){
				System.out.println(line);
			}
			input.close();
		}catch(Exception e) {e.printStackTrace();}
	}

}
